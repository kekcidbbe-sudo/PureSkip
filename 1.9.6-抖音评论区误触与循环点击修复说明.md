# 纯净跳过 1.9.6 抖音评论区误触与循环点击修复说明

## 问题结论

本次问题不是抖音专属，而是通用识别引擎在特定页面结构下可能发生的误触。抖音评论区同时包含频繁内容变化、输入框、输入法窗口和短文本控件，因此最容易触发。

主要原因：

1. 通用 `universal_ad_skip` 规则包含单独的“跳过”，但 1.9.5 的生效时间窗为 `0`，代表应用全程生效；普通页面中的“跳过”文字也可能被识别。
2. 输入法属于受限系统包，但旧流程会先把输入法当成 APP 切换，再检查是否受限，导致抖音会话的次数、冷却和启动时间窗被反复重置。
3. 规则命中后仍保留之前排队的重试任务，页面变化后可能继续识别。
4. 点击动作延迟 100～200 毫秒执行，但旧实现没有重新确认当前活动窗口和目标节点；页面已变化时存在旧位置误触风险。
5. 输入框及其子节点没有被候选识别层明确排除。

## 1.9.6 修复

- 通用“跳过”规则只在 APP 启动后 30 秒内生效，单次会话最多执行 3 次，冷却时间提高到 2 秒。
- 抖音自己的精确开屏规则仍保留，开屏广告首次识别和原有分段重试没有删除。
- 输入法和 System UI 作为临时覆盖窗口，不再重置当前 APP 会话。
- 切换到不允许处理的普通 APP、桌面或本应用时，取消未执行的扫描并清理当前会话。
- 规则命中后取消同批剩余扫描，避免一轮事件产生多次点击。
- 延迟点击前重新检查：
  - 当前会话包名；
  - 当前活动窗口包名；
  - 原目标节点包名；
  - 节点能否刷新且仍对用户可见。
- 编辑框、`EditText` 及其子树不再进入广告候选。
- 页面存在已聚焦输入框时，安全层阻止自动点击。

## 影响范围

- 抖音评论区、聊天输入区：重点修复。
- 其他视频、小说及小众 APP：同样获得保护，尤其是开启“增强识别”和“应用全程”的应用。
- APP 开屏广告：保留通用“跳过”识别，并提供 30 秒启动窗口；应用专属开屏规则和重试计划不变。
- APP 内晚于启动 30 秒出现、只显示单独“跳过”的广告：标准通用规则不再自动点；若确实需要，可使用按应用增强识别或手动教学。

## 已完成验证

- `./gradlew testDebugUnitTest`：43 个单元测试通过，0 失败。
- `./gradlew lintDebug`：通过。
- `./gradlew assembleDebug`：通过。
- APK：`com.nzsk.pureskip.debug`，`1.9.6-debug`，versionCode 19。
- APK 大小：15,412,636 bytes。
- SHA-256：`5143a559fbb79c836f69a233c3ba1187ff94862f9e35e766a4db9ca1fefd5d72`。
- 权限检查：没有 `INTERNET` 权限。

当前没有连接 Android 模拟器或真机，因此 `connectedDebugAndroidTest` 和抖音真实评论区回归尚未执行。

## 真机验收建议

1. 覆盖安装 1.9.6 调试版，重新确认无障碍服务状态。
2. 连续冷启动抖音 10 次，确认开屏广告识别没有退化。
3. 连续刷视频并查看评论至少 15 分钟：
   - 打开/关闭评论；
   - 点击输入框、弹出输入法；
   - 输入文字但不发送；
   - 收起输入法并继续滚动评论。
4. 分别测试增强识别关闭、启动阶段、应用全程三种配置。
5. 若仍出现一次误触，立即打开本地诊断并导出报告；报告只包含包名、事件、规则 ID、置信度和结果，不包含评论文字或截图。
